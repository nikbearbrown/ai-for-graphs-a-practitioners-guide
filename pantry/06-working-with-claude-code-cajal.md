# CAJAL Figure Intelligence — Chapter 6: Working with Claude Code

**Source:** `chapters/06-working-with-claude-code.md`
**Mode:** `/scan silent`
**Domain note:** AI+1 practitioner guide / data visualization with D3 and Claude. The pipeline chapter: four-move prompt structure (show, say, constrain, verify), MBTA iteration loop, Evergreen/Emery audit, five-stage pipeline. Four author-placed figures (numbered "5.1–5.4" in the source — chapter renumber pending).

---

## Density Recommendation

**4 figures, Process density.** Each figure makes a different process visible: the vague-vs-disciplined contrast, the four-move template, the iteration loop, the full pipeline.

---

## Zone Map

- **MC:** Four-move prompt structure (show, say, constrain, verify). MBTA iteration cycle (working chart → audit → targeted follow-up → publishable). Five-stage pipeline integrating Chapters 3, 4, 3, 5, audit.
- **VG:** Same-data side-by-side bar chart contrast. Bracketed prompt template. Circular loop diagram. Horizontal pipeline.
- **PQ:** Claude Code reliable-vs-unreliable table. Five-failure follow-up table.

---

## Figure 6.1 — Vague Prompt vs Four-Move Prompt, Same Data

**Priority: Critical.** The chapter's opening evidence. Side-by-side artifact contrast.

### Block 1 — Illustrae paste block

A two-panel side-by-side composition on white. Left panel: a vertical column-bar chart skeleton — five Vermillion `#D55E00` filled rectangles of uniform color, all the same hue (no luminance distinction), placed in source-file order (not sorted), columns of roughly equal width with a y-axis baseline that visibly sits well above the chart bottom (a Black `#000000` 1pt horizontal line drawn partway up the panel, indicating truncated baseline). Three of the columns crowd at the bottom of the axis. Left panel framed in light Vermillion `#D55E00` 8% opacity (the "vague" zone). Right panel: a horizontal bar chart skeleton — five Sky Blue `#56B4E9` to Blue `#0072B2` filled rectangles arranged top-to-bottom, sorted descending (longest at top), all sharing a zero baseline (Black 1pt vertical line at left edge), each bar's right end marked with a small Bluish Green `#009E73` filled tick (value annotation placeholder). Right panel framed in light Bluish Green `#009E73` 8% opacity (the "four-move" zone). White background, flat vector, double-column 174mm preferred.

### Block 2 — Full SCOPE prompt

[S] Double-column 174mm preferred, vector, white background.
[C] Two bar-chart skeletons. Left = vague prompt output: vertical, uniform color, source order, truncated baseline. Right = four-move prompt output: horizontal, sorted descending, sequential luminance, zero baseline, value-tick annotations.
[O] Side by side. Two equally sized panels with subtle background tinting.
[P] Vague panel bars Vermillion uniform. Four-move panel bars Sky Blue to Blue luminance ramp. Value ticks Bluish Green. Panel zones Vermillion 8% / Bluish Green 8%. Axis lines Black 1pt.
[E] No actual dataset values rendered, no axis tick text, no sector names, no title text, no decorative ornament, no shadows.

### Block 3 — Negative prompt

text labels, axis tick numbers, sector names rendered as graphics, dollar amounts, chart titles, gibberish letters, captions, decorative ornament, photographic elements, realistic 3D textures, drop shadows, gradient fills, gradient backgrounds, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 6.2 — Four-Move Prompt Template

**Priority: Important.** The protocol as a labeled object.

### Block 1 — Illustrae paste block

A vertical stacked composition resembling a code block silhouette. Four equal horizontal Sky Blue `#56B4E9` filled rounded rectangles stacked vertically, each separated by a thin Black `#000000` 1pt horizontal rule. To the left of each rectangle, a small Blue `#0072B2` filled square bracket marker indicating "Move 1" through "Move 4" (no numerals rendered — bracket size or thickness can distinguish them subtly). The third rectangle is taller than the others (Move 3 is the longest). To the right of each rectangle, a small Bluish Green `#009E73` filled circle annotation marker indicating the purpose of that move. The bottom rectangle (Move 4) has a thin Vermillion `#D55E00` 1pt outline on its right edge highlighting the verify step. White background, flat vector, single-column 89mm.

### Block 2 — Full SCOPE prompt

[S] Single-column 89mm, vector, white background.
[C] Four stacked move-blocks. Move 3 visibly tallest. Brackets left, annotation circles right. Verify move highlighted by a Vermillion outline.
[O] Vertical stack with thin horizontal separators.
[P] Move blocks Sky Blue. Bracket markers Blue. Annotation circles Bluish Green. Verify outline Vermillion. Separators Black.
[E] No prompt text rendered, no Move 1 / Move 2 labels rendered as text, no D3 code snippets, no decorative ornament, no shadows.

### Block 3 — Negative prompt

text labels, prompt content rendered as graphics, Move 1 / Move 2 numerals rendered, D3 code snippets, gibberish letters, titles, captions, decorative ornament, photographic elements, realistic 3D textures, drop shadows, gradient fills, gradient backgrounds, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 6.3 — The MBTA Iteration Loop

**Priority: Critical.** The cyclic process that distinguishes working from publishable.

### Block 1 — Illustrae paste block

A circular flow composition. Four nodes arranged in a clockwise cycle: top — a Sky Blue `#56B4E9` filled rounded rectangle (four-move prompt). Right — a Bluish Green `#009E73` filled rounded rectangle (working chart). Bottom — an Orange `#E69F00` filled rounded rectangle (audit). Left — a Vermillion `#D55E00` filled rounded rectangle (targeted follow-up). Curved Black `#000000` 1pt arrows connect them in clockwise rotation. From the audit node (bottom), a separate Blue `#0072B2` 1pt arrow exits the cycle downward to a fifth node — a larger Blue `#0072B2` filled rounded rectangle (publishable chart, the exit). The exit arrow is dashed to distinguish it from the loop. White background, flat vector, single-column 89mm.

### Block 2 — Full SCOPE prompt

[S] Single-column 89mm, vector, white background.
[C] Four-node clockwise cycle (prompt → chart → audit → follow-up → back to prompt). Audit node has a dashed exit branch to a publishable-chart node below.
[O] Circular arrangement; exit below.
[P] Prompt Sky Blue. Working chart Bluish Green. Audit Orange. Follow-up Vermillion. Publishable Blue. Loop arrows Black solid. Exit arrow Blue dashed.
[E] No node labels rendered as text, no Barry-Card quote, no timing annotations, no decorative ornament, no shadows.

### Block 3 — Negative prompt

text labels, node names rendered as graphics, Barry-Card quote, timing annotations, twelve-seconds badge, gibberish letters, titles, captions, decorative ornament, photographic elements, realistic 3D textures, drop shadows, gradient fills, gradient backgrounds, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 6.4 — Five-Stage Pipeline

**Priority: Important.** The horizontal integration view from data audit to publishable chart.

### Block 1 — Illustrae paste block

A horizontal left-to-right flow composition. Five rounded rectangles arranged in a row, connected by Black `#000000` 1pt arrows. Stages 1, 2, 3 are Sky Blue `#56B4E9` filled (upstream chapters). Stage 4 is Blue `#0072B2` filled (this chapter, slightly taller/wider to emphasize). Stage 5 is Bluish Green `#009E73` filled (audit and iterate, with a small Vermillion `#D55E00` 1pt dashed feedback arrow curving back from stage 5 to stage 4 above the row). Each rectangle has a small Orange `#E69F00` filled circle below it (the deliverable file marker). White background, flat vector, double-column 174mm preferred.

### Block 2 — Full SCOPE prompt

[S] Double-column 174mm preferred, vector, white background.
[C] Five-stage horizontal pipeline. Upstream stages 1–3 same color; this-chapter stage 4 emphasized; audit stage 5 closes with a dashed feedback loop to stage 4.
[O] Left-to-right linear flow. Deliverable markers below each stage. Feedback arc above stage-4-to-5.
[P] Upstream stages Sky Blue. This chapter stage Blue. Audit stage Bluish Green. Deliverable markers Orange. Feedback arrow Vermillion dashed. Connecting arrows Black solid.
[E] No stage labels rendered as text, no filename text, no Chapter-3 / Chapter-4 numerals rendered, no decorative ornament, no shadows.

### Block 3 — Negative prompt

text labels, stage names rendered as graphics, chapter numerals rendered, filename text, deliverable text, gibberish letters, titles, captions, decorative ornament, photographic elements, realistic 3D textures, drop shadows, gradient fills, gradient backgrounds, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## What CAJAL Declines to Architect

- **Claude Code reliable-vs-unreliable table (lines 111–117).** Typography reference table.
- **Five-failure follow-up table (lines 192–198).** Typography reference table.
- **Code blocks (lines 23–48, 288–319).** Prose-with-monospace, not figures.
- **AI Wayback portrait of Grace Hopper.** Editorial portrait, separate pipeline.

---

## Video Candidate Pass

**FIGURE 6.1 (vague vs four-move):** STATIC SUFFICIENT.
**FIGURE 6.2 (four-move template):** STATIC SUFFICIENT.
**FIGURE 6.3 (MBTA loop):** **MILD VIDEO CANDIDATE.** Cyclic-process criterion met. Animation showing one trip around the loop — prompt fires, chart appears, audit flags an issue, follow-up corrects it, exits to publishable — could dramatize the iteration discipline. Optional.
**FIGURE 6.4 (pipeline):** STATIC SUFFICIENT.

**Video candidates identified: 1 mild.** Recommended only if a moving demonstration of iteration adds pedagogical value beyond the static loop.

---

## Split-point note

Chapter cross-references Chapters 3, 4, 5 (upstream pipeline) and Chapter 14 (full Evergreen/Emery checklist). The "twelve seconds" framing recurs and could be shown numerically in companion typography, not in the figure. The figure numbering in source ("5.1–5.4") appears to be a copy-paste artifact — actual chapter is 6.
